# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
on:
  push:
    branches:
      - main

env: 
  VITE_REACT_APP_TESTHEADER: ${{ secrets.VITE_REACT_APP_TESTHEADER }}

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      # - run: |
      #     touch .env
      #     echo VITE_REACT_APP_TESTHEADER=${{ secrets.VITE_REACT_APP_TESTHEADER }} >> .env
      #     echo FIREBASE_SERVICE_ACCOUNT_LAB_INVENTORY_TRACKER=${{ secrets.FIREBASE_SERVICE_ACCOUNT_LAB_INVENTORY_TRACKER }} >> .env
      #     cat .env        
      #     npm ci && npm run build
      - run: npm ci && npm run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_LAB_INVENTORY_TRACKER }}
          channelId: live
          projectId: lab-inventory-tracker
